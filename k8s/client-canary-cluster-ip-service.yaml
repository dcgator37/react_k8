apiVersion: v1
kind: Service
metadata:
  name: client-canary-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: web
    version: canary
  ports:
    - port: 3000
      targetPort: 3000